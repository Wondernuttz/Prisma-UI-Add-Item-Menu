; PrismaUIAddItemMenu defaults.
; The toggle hotkey is stored in the ESP global PrismaUIAddItemMenu.esp|800
; so MCM and the SKSE plugin read the same live value.

[General]
iPageSize = 100
bPauseWhileOpen = 0
bExcludeVanilla = 0
